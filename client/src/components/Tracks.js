import React from "react";
import styled from "styled-components/macro";
import theme from "../style/theme";

const { colors, fontSize, spacing } = theme;

const Tracks = () => {
  return <h1>In tracks</h1>;
};

export default Tracks;
